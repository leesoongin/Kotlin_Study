package Kotiln_Youtube_Study

/*스코프와 접근제한자*/

/*변수나 함수 클래스같은 멤버들을 서로 공유하여 사용할수있는 범위를 지정해 둔 단위
*스코프가 지정되는 범위는
패키지 내부 클래스 내부 함수 내부 등이 있다.
*
* 접근제한자 in 패키지 스코프
* public(기본값) 어떤 패키지에서도 접근 가능
* internal 같은 모듈 내에서만 접근 가능
* private 같은 파일 내에서만 접근 가능
*
*  ... in 클래스 스코프
* public(기본값) 클래스 외부에서 늘 접근 가능
* private 클래스 내부에서만 접근가능
* protected 클래스자신과 상속받은 클래스에서만 접근가능
 */
fun main(){
    println("이론적인부분인데 대충하고 넘어가고 나중에 필요할때에 다시 보는게 나을듯 지금은 대충 어떤건지 이해만하고 넘어감")
}